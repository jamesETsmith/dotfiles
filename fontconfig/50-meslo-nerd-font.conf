<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Prefer MesloLGS NF for terminal monospace rendering -->
  <alias>
    <family>monospace</family>
    <prefer>
      <family>MesloLGS NF</family>
    </prefer>
  </alias>

  <!-- Nerd-patched Meslo can be misclassified as proportional -->
  <match target="scan">
    <test name="family">
      <string>MesloLGS NF</string>
    </test>
    <edit name="spacing" mode="assign">
      <int>90</int>
    </edit>
  </match>
</fontconfig>
